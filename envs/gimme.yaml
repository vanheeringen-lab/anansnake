name: ananse
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - gimmemotifs-minimal =0.17.2
  - gffread
  - orthofinder
  - xgboost  # optional maelstrom dependency

  # TODO: remove with gimmemotifs-minimal =0.18.0
  - pip
  - pip:
      - git+https://github.com/vanheeringen-lab/gimmemotifs.git@develop
